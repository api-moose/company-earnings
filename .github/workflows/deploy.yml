name: Deploy to Google App Engine

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: app-engine

    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.22.4'

    - name: Install dependencies
      run: go mod download

    - name: Create .env file
      run: |
        echo "FIREBASE_CREDENTIALS_FILE=placeholder" > .env
        echo "PORT=8080" >> .env

    - name: Run tests
      env:
        FIREBASE_CREDENTIALS_FILE: ${{ secrets.FIREBASE_CREDENTIALS_FILE }}
      run: go test ./...

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v1
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Set up Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v1
      with:
        project_id: ${{ secrets.GCP_PROJECT }}

    - name: Grant Storage Object Viewer permission
      run: |
        gcloud projects add-iam-policy-binding ${{ secrets.GCP_PROJECT }} \
          --member=serviceAccount:${{ secrets.GCP_SA_EMAIL }} \
          --role=roles/storage.objectViewer

    - name: Deploy to Google App Engine
      run: |
        echo "${{ secrets.FIREBASE_CREDENTIALS_FILE }}" > /tmp/firebase_credentials.json
        gcloud app deploy app.yaml --quiet
      env:
        FIREBASE_CREDENTIALS_FILE: /tmp/firebase_credentials.json